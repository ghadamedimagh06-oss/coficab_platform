# Default backend URL for local development (committed, so a fresh clone works
# without manual setup). Override per-machine in .env.local if your backend
# runs elsewhere. Must match the backend port from the README:
#   python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
NEXT_PUBLIC_API_URL=http://localhost:8000
